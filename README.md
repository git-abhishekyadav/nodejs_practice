# nodejs_practice
REPL ---- READ EVAL PRINT LOOP 
  a shell env to type code and immediately see the result

Every file is module in nodejs and it gets commonJs module features
and is converted to wrapper function by nodejs
